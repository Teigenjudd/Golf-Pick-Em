# Poold — Product Roadmap

> **What this file is:** The prioritized plan — where we can win, what we build next,
> and how hard each thing is. Reference it at the start of every build session; update
> statuses as items ship and re-rank when strategy changes. What the product *is* today
> lives in `PRODUCT.md`; engineering-level defects live in `docs/BACKLOG.md` (IDs like
> A1/B2 below point there).
>
> **Last updated:** 2026-07-15 (C1 sign-in dead-end fixed, PR #27. Shipped roadmap items
> moved out of the P0–P3 tables into a "✅ Shipped" list; P0.2 self-serve creation is still
> the whole critical path. See the status log.)
>
> **Ease scale:** 🟢 Easy (a session or two) · 🟡 Moderate (several sessions, one
> surface) · 🔴 Hard (multi-PR, schema + UI + new moving parts)
> **Impact:** judged against the three questions from PM.md — does it acquire
> commissioners, retain players, or cut friction?

---

## 1. Market snapshot (July 2026)

Researched: Splash Sports, RunYourPool, Majors Challenge, RunMyPools, EasyOfficePools.

- **Splash Sports** is the funded, real-money operator. Their golf lineup: tiered
  majors pools (six tiers, pick one per tier — *the same format we run*), One & Done
  (season-long, huge — $3,500-entry flagship with $1M+ guaranteed), Survivor, DFS.
  They handle entry collection and payouts — which is exactly why they carry state
  restrictions, KYC, and rake. They validate our format and can't follow us down the
  free/no-money path without breaking their business model.
- **RunYourPool** (multi-sport) and **Majors Challenge** (golf-first) both charge the
  commissioner ~$1 per entry. Both are feature-rich (message boards, multi-event
  season standings, custom scoring) and both look dated. Majors Challenge sells a
  $599/yr unlimited product to golf clubs and white-labels pools for brands — proof
  the club/course channel we're targeting has money in it.
- **RunMyPools / EasyOfficePools** are the free/cheap tail — templated, utilitarian.
- **Format landscape:** tier pools, One & Done, and majors-series (aggregate standings
  across the four majors) are the three formats that dominate golf pools. We have the
  first. We have nothing season-long — and season-long is what keeps a group alive
  between events.

**Conclusions — where Poold can win:**
1. **The friend-group experience.** Everyone else optimizes for pool *administration*;
   nobody owns the Saturday-afternoon *feeling*. Design, mobile feel, and social energy
   are our differentiation — lean in.
2. **Free + legal simplicity.** Incumbents charge the exact person we need to win
   (the commissioner); Splash carries regulatory weight everywhere. "Free, beautiful,
   no legal weirdness" is the pitch.
3. **Season-long play is the retention unlock.** A single-event pool dies Sunday
   night. A majors-season or One & Done pool keeps the group chat alive from April to
   July. This is the biggest format gap between us and every competitor.
4. **The club/bar channel is real** (Majors Challenge proves it) — but only after
   self-serve pool creation exists.

---

## 2. The blunt strategic read

Our stated strategy is "win the commissioner — they bring the group."
**Today, nobody but the founder can create a pool.** Pool creation is gated behind the
global admin role. Until that changes, Poold has no acquisition motion at all — every
other growth idea is downstream of this one fact.

**Updated 2026-07-14:** the two security items that made a public push irresponsible
(A1, A2) are now **fixed**. That sharpens the read rather than softening it — the reason
we can't tell anyone about Poold is no longer *"it isn't safe."* It's that **the product
has no way for them to use it.** P0.2 (self-serve creation) is now the whole of P0's
critical path, with P0.5 (Supabase auto-pause) as the remaining infrastructure floor.

---

## 3. Roadmap

### P0 — Make it safe and make it self-serve *(prerequisite for telling anyone about Poold)*

| # | Item | Why | Impact | Ease |
|---|---|---|---|---|
| 0.2 | **Self-serve pool creation** — any user can create a pool and becomes its commissioner; per-pool commissioner powers (lock/close/refresh/manage participants for *their* pool); global admin stays for ops | Turns the product from founder-run to commissioner-run. THE strategic unlock — without it there is no growth loop. | Very High | 🔴 (roles model + RLS + admin-UI split into "my pools"; the create wizard itself already exists and mostly just needs un-gating) |
| 0.4 | **Error states instead of blank screens** (C2, B2; ~~C1~~ ✅ done) | A commissioner's first bad experience shouldn't look like a broken product. Silent failures are trust killers for exactly the audience we can't afford to lose. | High | 🟡 (thread errors through `lib/golf.js` + a few UI states) |
| 0.5 | **Stop Supabase auto-pausing the project** — upgrade to Pro (~$25/mo), or run a year-round heartbeat so the DB never idles out | **Proven failure, 2026-07-13:** the free tier paused after ~7 days idle, Supabase pulled the project's DNS, and getpoold.app died at sign-in with an opaque "load failed." Any gap between tournaments is long enough to trigger it — so an invite that lands in a quiet week reaches a dead app. This is the single cheapest way to stop losing users we've already acquired. | High (risk removal) | 🟢 (Pro is a billing toggle; the heartbeat is a scheduled function) |

### P1 — Sharpen the growth loop *(make every invite and every weekend better)*

| # | Item | Why | Impact | Ease |
|---|---|---|---|---|
| 1.2 | **Pick-deadline reminder emails** — "picks lock in 24h and you haven't submitted" | The #1 commissioner pain in every pool is chasing stragglers. Automating the nudge is a retention AND commissioner-love feature. Table stakes at competitors. | High | 🟡 (Supabase scheduled edge function + email; magic-link infra means we already send email) |
| 1.3 | **Live-feel leaderboard** — auto-refetch on interval/focus while an event is in progress, subtle "updated Xm ago" | Users park on this page all weekend; today they must hard-refresh. The product's core moment should feel alive. | Medium-High | 🟢 (client-side refetch of the existing cache; no new infra) |
| 1.4 | **Score-swing / final-result email or share moment** — "You moved to 2nd" or a shareable final-standings card | Brings people back mid-weekend and gives winners something to rub in — trash talk is the product. | Medium | 🟡 |
| 1.5 | **Unmatched-pick flagging (B1) + DQ/DNS handling (B3)** | Scoring integrity is the whole product promise; silent mis-scoring during a live event is a group-chat catastrophe. | Medium | 🟢 |

### P2 — Retention formats *(keep the group alive between Sundays)*

| # | Item | Why | Impact | Ease |
|---|---|---|---|---|
| 2.1 | **Season / series pools** — one pool spanning multiple events (e.g. all four majors) with aggregate standings | The single biggest format gap vs. every competitor. Converts a one-weekend group into an April-to-July group. Schema hint: `events` is already separate from `pools`; this needs a pool↔many-events link + season standings. | Very High | 🔴 |
| 2.2 | **One & Done format** — season-long, pick one golfer per event, can't reuse | The most popular season format in golf pools (Splash's flagship). Second contest type proves the multi-format seam. | High | 🔴 (new format logic; reuses events/leaderboard infra) |
| 2.3 | **In-pool chat or reactions** — even a lightweight emoji-on-scorecard layer | "Social energy over utility" is a principle; today all trash talk happens off-platform. Start tiny (reactions), not a full chat. | Medium | 🟡 (Supabase realtime exists; scope discipline required) |
| 2.4 | **Pool history / archive** — past pools with final standings, "champion" badges | Gives pools a memory and commissioners bragging-rights continuity. Feeds 2.1. | Medium | 🟢 |
| 2.5 | **Configurable tier format (G1)** | Commissioners of small/large groups need different shapes; currently hardcoded 6×6+2 wildcards. | Medium | 🟡 |

### P3 — Channel + platform expansion *(after golf is demonstrably won)*

| # | Item | Why | Impact | Ease |
|---|---|---|---|---|
| 3.1 | **Club/bar onboarding** — a "run your club's Masters pool on Poold" page + white-label-lite (logo/name on the pool) | Majors Challenge charges $599/yr for this; we can land the channel free and monetize later. Needs 0.2 first. | High | 🟡 |
| 3.2 | **PWA / add-to-home-screen** — installable, app icon, offline shell | Mobile-first product with no app-store presence; PWA is 80% of native's value at 5% of cost. | Medium | 🟡 |
| 3.3 | **Multiple pools per event (UI)** — schema already supports it | Lets a bar run a free pool and a stakes pool on the same US Open; also the seam for public "featured event" pools someday. | Medium | 🟡 |
| 3.4 | **Second sport — CFB, cut over to prod (PR0 shipped 2026-08-11, cutover shipped 2026-08-15)** — college football, weekly against-the-spread/season-cumulative, full schema/UI/data-provider siloing (`docs/CFB_FORMAT.md`, `docs/CFB_BUILD_PLAN.md`, ~11-PR sequence) | Chosen deliberately ahead of golf being fully "proven" — supersedes this row's earlier NFL placeholder; see DECISIONS 2026-08-11. Every player and admin screen is built — data layer, scoring/grading, live scores, admin (including a sport-agnostic create chooser + Golf|CFB switcher, and now a "Close Pool" control), dashboard/join reachability, the weekly picks builder plus its read-only locked/auto-filled/graded card, and automatic missed-deadline handling — every core build PR is shipped, and as of PR #58 (2026-08-15) the prod cutover is done too: all four CFB edge functions are deployed and all four CFB cron jobs (the three billable pollers plus the free auto-fill/lock cron) are armed and active. No real season has run through it yet — this item's remaining risk is entirely "no real users," not "not built." | High (later) | 🟢 |
| 3.5 | **Monetization decision** — likely: free forever for friends, paid tier for clubs/brands (white-label, custom formats) | Don't charge the commissioner we're trying to acquire; charge the business that uses pools as engagement. Decide only with channel data from 3.1. | — | (decision, not build) |

### Engineering hygiene (running alongside, not a phase)
Phase 5 legacy-table cleanup + `pool_standings` decision (F1) · tests for
`scoring.js`/`tierBuilder.js` (F4) · a11y pass (E3) · 404 route (C3). Pull one in
whenever a PR is in the neighborhood.

### ✅ Shipped (pulled out of the P0–P3 tables above)
IDs kept so other docs' references still resolve; the tables above now show only open
work. Full narrative is in §5 Status log.
- **0.1 — Fix A1 privilege escalation** — PR #24, 2026-07-14. `profiles` column-locked via GRANTs; role changes go through the `admin_set_role()` SECURITY DEFINER RPC.
- **0.3 — A2: move Odds API key server-side** — PR #24, 2026-07-14. `odds-proxy` edge function; key is now a Supabase secret. ⚠️ old key assumed burned (rotation is a manual deploy step).
- **1.1 — Invite link preview (OG tags)** — PR #26, 2026-07-14. Per-pool dynamic card rewritten on `/join/*` by a Netlify edge function; `/demo` gets its own pitch. Per-event images remain open (H5).
- **F3 — Geocoding switched from Open-Meteo to Nominatim** — PR #31, 2026-07-16. `CreateTournament` now resolves course coordinates via Nominatim free-text search instead of Open-Meteo's name-only match, fixing UK links courses (The Open @ Royal Birkdale) that previously came back with null lat/lon and a blank weather widget.

---

## 4. Sequencing logic (why this order)

1. **P0 before any acquisition effort** — a security hole plus founder-gated pool
   creation means marketing would be wasted or dangerous.
2. **P1 before P2** — sharpen the loop for the pools that already happen (majors come
   4× a year; each is a growth event) before building new formats.
3. **2.1 (season pools) is the single most valuable post-P1 build** — it multiplies the
   value of every other retention feature by keeping groups alive between events.
4. **Monetization stays unresolved on purpose** until the club channel produces data.
   Free is a weapon right now; don't sheath it early.

---

## 5. Status log

| Date | Change |
|---|---|
| 2026-07-10 | Initial roadmap from market research + full repo read. Nothing started; P0 defined as A1 + self-serve creation + A2 + error states. |
| 2026-07-13 | **Shipped: tournament badge color system** (Claude Design import). Badge bg/border are now per-tournament and encode prestige + geography; all 48 events designed and seeded. Not on this roadmap when written — it's craft/polish rather than a P0–P3 item, but it lands squarely on differentiation #1 (the friend-group *feel*), so it earns its place. |
| 2026-07-13 | Supabase free tier **auto-paused the project** after ~7 days idle, taking getpoold.app down with an opaque "load failed" at sign-in. Restored manually. This is a live launch risk, not an annoyance — see the new item **0.5** in P0. |
| 2026-07-14 | **Shipped: P0.1 (A1) + P0.3 (A2)** — PR #24. The self-promotion hole is closed and the Odds key is off the client. Two consequences worth naming. **(1) The blunt read changed:** the blocker on telling anyone about Poold is no longer safety, it's that only the founder can create a pool — P0.2 now *is* the critical path. **(2) A pattern was established, not just a bug fixed:** RLS can't restrict columns, so column access is a GRANT problem, and privileged operations belong in `SECURITY DEFINER` RPCs that re-check `is_admin()`. That's now the house rule for every future privileged write (see DECISIONS). |
| 2026-07-14 | **Logged, not shipped: `docs/BRAINSTORM.md`** — a ~120-idea product dump (multi-sport, formats, social, live UI, data, brand, growth, retention, monetization) with an anti-ideas list. It is deliberately **not** a roadmap; it's the pile this roadmap can pull from. Four of its ideas challenge things written here and want an explicit decision: that the second sport should be a *field* sport (F1/UFC — which reuse golf's schema) rather than NFL (which reuses none of it); that a **Crew** object would make season pools (2.1, rated 🔴) mostly a query; that "run it back" is a 🟢 retention win we're leaving on the table; and that the tagline implies a category ("make it interesting") much larger than *sports*. |
| 2026-07-14 | **Shipped: P1.1 — invite links unfurl as a real card** (PR #26). The join link is the entire funnel and it was arriving in group chats as a bare URL. The cause was structural, not cosmetic: crawlers read `<head>` without running JavaScript, and an SPA has nothing in `<head>` — no amount of React could have fixed it. A Netlify edge function now rewrites the OG tags before the HTML is sent, so an invite reads *"Judd invited you to The Open Championship — 8 picks. No app, no password, no download."* We went past the 🟢 static version straight to the per-pool dynamic card, and `/demo` got its own pitch. **Two things worth carrying forward.** (1) This is the first thing we ship that puts a user's display name in front of *people who aren't in the app yet* — which retroactively makes the same morning's display-name work part of the growth loop, not just a privacy fix. (2) We declined to give the edge function a service-role key and built a narrow `SECURITY DEFINER` RPC instead (see DECISIONS) — the pattern to reuse the next time something outside the app needs to read something inside it. Remaining: per-event card images (**H5**). |
| 2026-07-14 | **Shipped: user-set display names + the legal pages** (PR #25). Two things, one PR. **(1)** Display names had been auto-seeded from the email local-part since June, so every leaderboard was publishing part of each player's email address to their pool. New accounts now hit a `/welcome` wall and choose a name; existing users get a nudge on the "You" tab (`/profile`) rather than a forced rename — sessions persist, so a login-time prompt would never have reached them. This adds a step to the invite funnel, which is the growth loop, so watch it. **(2)** `/privacy` and `/terms` now exist and say the thing that protects us: Poold never touches money. That was a product principle with no document behind it; now the Terms describe the code — which means **any future payments feature must change the Terms before it ships.** New risk logged: the contact address in both documents (`privacy@getpoold.app`) **doesn't exist yet** — the domain has no MX record, so mail bounces (**BACKLOG A7**, 🟠, accepted knowingly). |
| 2026-07-15 | **Shipped: C1 — AuthCallback sign-in dead-end** (PR #27). A friend joining via a shared Instagram-story link got stranded on the "Signing you in…" spinner; his account *was* created, he just couldn't get off the page. Root cause was a race, not the browser: on a fresh signup the `profiles` row is created by a DB trigger a beat after the session exists, so the first read came back null, and `AuthCallback` only advanced once `profile` was non-null — with no retry and no timeout. Fix: `fetchProfile` now retries through the trigger gap, and the callback advances on the *session* and shows a "Back to sign in" fallback after 8s so it can't spin silently. **Worth carrying forward:** this is a live cost of the invite→magic-link funnel we just polished (P1.1) — the first thing a *new* invitee does is exactly the path that was broken, and in-app browsers (Instagram, etc.) make the race easier to lose. A durable fix for social traffic is OTP-code login (type a 6-digit code) instead of a click-through link that can open in a different browser — not built, now tracked as **BACKLOG C6** (⚪); flag if signup drop-off shows up. |
| 2026-07-13 | **Shipped: odds coverage fix** (PR #22). Odds were read from one bookmaker and joined to the field by exact name — 11 of 100 players in The Open had no price (incl. Tom Kim, both Højgaards) and silently fell back to OWGR interpolation for tiering. Now unions all books (median price) and matches names in layers. **100/100 priced.** Not a roadmap item, but it was quietly degrading the tier builder — the admin's first real impression of the product — and the diagnosis exposed that we have no test coverage on this path (**F4**). |
| 2026-07-15 | **Shipped: per-tournament poll dedupe + admin polling toggle; branded auth email written but blocked on SMTP** (PR #29). Three things. **(1)** `poll-leaderboard` now hits the Slash Golf API **once per real tournament** and fans the result into every pool's event cache, instead of once per pool — The Open was burning 7 calls/cycle for 7 pools on one tournament; now 1. API spend scales with tournaments, not pools. **(2)** The morning-of cron ritual (paste four `cron.schedule` blocks with the secret, unschedule after) is now an **on/off toggle in the Admin panel**, backed by admin-only RPCs that read the secret from Supabase Vault. Deliberately gated on `is_admin()` rather than a hardcoded owner email — a future commissioner role is a *different* role, so the server already refuses it and the eventual split only needs to hide the card. **(3)** A branded Poold magic-link email was written (`supabase/templates/magic_link.html`), but current Supabase **gates template editing behind custom SMTP**, so it can't be applied until SMTP is set up — new **BACKLOG C7**, which also fixes the generic `@supabase.io` sender + spam problem and overlaps A7's missing mail infra. Also logged **F6** (extract a format-engine contract before format #2, not sport #2) and an enterprise-architecture reference doc, both out of a scalability review. |
| 2026-07-16 | **Shipped: F3 — geocoding switched from Open-Meteo to Nominatim** (PR #31). Open-Meteo's geocoder matched on the `name` field only and couldn't resolve golf-course names or "City, State" strings, so UK links courses got no coordinates and the weather widget rendered blank (The Open @ Royal Birkdale — those `event_details` rows were backfilled by hand outside this PR). `CreateTournament` now queries Nominatim with `courseName, city, state`, falling back to a town-level `city, state` query if the specific one misses, plus the `email=` param Nominatim's usage policy asks for. Small hygiene item, not a roadmap phase, but it closes a real gap for future major-championship creations on non-US courses. |
| 2026-07-16 | **Shipped: branded auth email, closes C7** (PR #34). Picks up where PR #29 left off: custom SMTP is now live (Resend, verified on `getpoold.app` — SPF/DKIM/DMARC), so auth mail sends from `login@getpoold.app` instead of the rate-limited default Supabase sender, and the branded Magic Link template is pasted into the dashboard and confirmed end-to-end. The PR's only code change was syncing `supabase/templates/magic_link.html` to the live copy ("You're one tap in." → "Tap in.") — the SMTP/DNS/dashboard work happened outside the repo. This closed the *sending* half of `getpoold.app` mail only; the *receiving* half (**A7**, `privacy@getpoold.app` still bounces — no inbound MX) is a separate DNS task and remains open, queued next. |
| 2026-07-17 | **Shipped: Claude Design sync scaffolding, no app code touched** (PR #35). All 15 shared UI components (`src/components/**`) are now wired into a claude.ai/design project — `.design-sync/` config, a named-export barrel, the compiled Tailwind CSS, 15 authored preview cards, and generated preview data (`src/demo/demoData.js` snapshot) — so the design agent iterates against Poold's real components and brand tokens instead of a from-scratch mock. Two preview-only shims (a supabase stub, an ASCII-safe copy of `scoring.js`) exist because the design-sync bundler can't load the real `lib/supabase.js` (throws on missing env) or `scoring.js` (literal non-ASCII regex chars break a non-UTF-8 load path) — both are scoped to the tool's own tsconfig paths and provably invisible to `vite.config.js` (confirmed in senior review). This is the same "Claude Design" pipeline that produced the 2026-07-13 badge color system, now generalized into a repeatable sync instead of a one-off import. Not on this roadmap's P0–P3 list — it's tooling, not a user-facing ship — logged because it changes how future design work gets done. See DECISIONS and **BACKLOG F7** (the latent scoring.js regex bug the shim works around). |
| 2026-08-11 | **Planning shipped, not code: CFB (sport #2) format + build plan** (PR0, PR #39, docs-only). `docs/CFB_FORMAT.md` is the rules-of-record — weekly against-the-spread, season-cumulative, all-or-nothing submission with full random auto-fill on a missed deadline, single join cutoff before Week 1. `docs/CFB_BUILD_PLAN.md` sequences the build into ~11 PRs: full sport-layer siloing (own `cfb` schema, `lib/cfb.js`, scoring engine, picks UI, `cfd-proxy`) over the shared neutral `public` core, with only the `pool_standings` output shape shared between sports (wires up F1's dead table) and no FormatEngine abstraction at two formats (F6 stays deferred to format #3). Chosen **deliberately against** every strategy doc's own recommendation to pick a golf-shaped sport #2 first (`docs/BRAINSTORM.md` Obs.1/§A, `docs/MULTI_SPORT_MIGRATION.md` D1 both flag team sports as the most expensive option) — a founder call, eyes open (see DECISIONS). Supersedes item **3.4**'s earlier "NFL likely" placeholder. **Execution has not started** — PR1 (`cfb` schema scaffold) is next, not gated on anything else in this roadmap. |
| 2026-08-11 | **Shipped: CFB PR1 — `cfb` schema scaffold** (PR #40). The first CFB code PR: an additive-only migration creates the empty `cfb` schema (`event_details`, `weeks`, `games`, `picks`), seeds `public.sports` for `'cfb'`, grants API roles, and enables RLS with no policies (deny-all while empty) — plus `cfb` added to `supabase/config.toml`'s exposed schemas for local dev only (the prod dashboard flip stays a deferred cutover step). Nothing golf reads is touched. Senior review flagged four cheap-now/expensive-later integrity gaps against the build-plan's schema sketch, and all four went in before merge: a uniqueness constraint on `cfb.weeks(event_id, week_number)`, a composite FK forcing a pick's `game_id` to actually belong to its `week_id`, `CHECK` constraints on the status/result enum columns, and `season_year NOT NULL`. RLS policies, the `cfb_submit_week_picks` RPC, slate import, scoring, grading, and UI remain PR2+. |
| 2026-08-11 | **Shipped: CFB PR2 — RLS policies + the `cfb_submit_week_picks` submit RPC** (PR #41). Reference tables (`event_details`/`weeks`/`games`) get authenticated-read + admin-manage policies mirroring golf phase-3; `cfb.picks` gets read-own-anytime + read-others'-confirmed-picks-after-that-week's-lock (per-week, not per-pool). The bigger call: **no client insert/update/delete policy on `cfb.picks` at all** — a deliberate change from the build plan's original "defense-in-depth row policies" sketch, because a per-row policy structurally cannot enforce "these 6 rows are one legal card," so adding one would reopen the hole rather than harden it (see DECISIONS). The `cfb_submit_week_picks()` RPC is therefore the sole write path: validates membership, week-belongs-to-season, week-still-open (gated on `lock_time`, not a `status='open'` flag — also a logged founder decision), and the full 6-pick card shape, then atomically replaces it. Senior review (APPROVE WITH QUESTIONS) traced every malformed-card case and confirmed the security properties hold; flagged a forward data contract for PR3 (`cfb.games.underdog_team`/`underdog_spread` must be the real underdog, stored positive, non-NULL — now written into `docs/CFB_BUILD_PLAN.md`'s PR3 row). Already applied to prod (`supabase db push`); `cfb` stays hidden from the Data API until the Exposed Schemas dashboard flip. |
| 2026-08-13 | **Shipped: CFB slate import automated, replacing PR #46's manual per-week import** (PR #47, `cfb-auto-lines-poller`). Real 2025 season data showed betting lines only post ~1-2 weeks before kickoff (Week 1 = 51 games with a posted line, Week 3 = 0), so a season's slate can't be imported upfront — the admin "Import slate" button is gone, replaced by `poll-cfb-lines`, an hourly service-role poller that pulls current games/spreads for every active season and fans them onto every pool's weeks, deduped. **CFB now has no admin "odds" step at all** — spreads are the CFBD lines, pulled automatically. Each game's spread freezes at kickoff (the closing line, shown forever after) and a new `cfb.spread_history` table logs line moves for a future chart. The CFBD→`cfb.games` transform moved server-side and got its first committed unit tests, which caught a real bug (a lineless game read as a phantom pick'em `0`); 173 tests pass repo-wide. Not yet deployed/armed — deferred to PR9, which now arms three CFB crons (slate/lines, live scores, grading) instead of two. See DECISIONS, 2026-08-13. |
| 2026-08-13 | **Shipped: CFB player UI Phase 1 of 4 — locked "Varsity Navy" theme + shared-shell prop-ification + placeholder routes** (PR #48). The first CFB *player-facing* UI work (prior CFB PRs were backend + admin, item **3.4**). `src/theme/cfb.js` locks the colorway (navy header gradient, brick accent, green cover/win) from a founder-approved Claude Design comp. The shared `PoolHeader`/`PicksHeader`/`StandingsCard`/`WidgetGrid` shells — previously hardcoded to golf's fairway/gold — are now prop-ified with golf's exact values as defaults, so golf is unaffected (confirmed byte-identical in senior review). Two new routes, `/cfb/pool/:id` and `/cfb/pool/:id/picks`, render the real themed shell over a "coming soon" body; neither is linked from the dashboard yet. Real content (standings = Phase 2, picks builder = Phase 3, dashboard reachability = Phase 4) is still open. Full phase plan + rationale in DECISIONS, 2026-08-13. |
| 2026-08-13 | **Shipped: CFB player UI Phase 2 of 4 — the real Pool Detail/leaderboard body** (PR #49). `/cfb/pool/:id` now shows the real product: a season-cumulative standings hero (ranked by `public.pool_standings.total`, never re-ranked by week), a week selector that scopes — without re-ranking — a scorecard-expand and a CFB widget row (this week's slate, weekly points, most-backed teams, underdog board, prize pool) to one chosen week. Four new read-only `lib/cfb.js` queries feed it. Senior review (APPROVE WITH QUESTIONS, no blockers) traced RLS visibility and the client-side grade and found no leaks; it surfaced two presentation questions the founder resolved in-branch (DECISIONS, 2026-08-13): the weekly points total is recomputed live in the browser off game scores for instant feedback rather than waiting on the server poll (can briefly lead the season total, self-corrects), and the three pick-derived widgets are hidden behind a "reveal when Week N locks" note before a week locks (RLS only returns your own picks pre-lock, so showing them would read as a near-empty pool). Picks builder (Phase 3) and dashboard reachability (Phase 4) still open. |
| 2026-08-13 | **Shipped: CFB weekly picks builder, PR-A** (`feat/cfb-picks-builder`). `/cfb/pool/:id/picks` is no longer a placeholder — it's the real card builder: 5 ATS picks + an optional double-down + a mandatory underdog pick on 6 distinct games, live client-side validity mirroring the `cfb_submit_week_picks` RPC (the real gate), re-submittable until the week locks. Senior review (APPROVE WITH QUESTIONS, no blockers) confirmed the line-sign math and UI-validity/RPC parity are both correct. |
| 2026-08-14 | **Shipped: CFB read-only picks card, PR-B** (PR #55, `feat/cfb-picks-readonly`). `/cfb/pool/:id/picks` now renders a frozen read-only card once a week locks (locked/submitted, locked/auto-filled, graded) via the new `CfbCardReadonly`, sharing its row renderer (`CfbCardRows`) and grading shape (`shapeCard`, lifted out of `CfbPoolDetail`) with the pool-detail scorecard-expand so the two views can't drift — closing item **3.4**'s last open build item. Same PR also shipped a sport-agnostic admin entry point (`/admin/create`, choose Golf or CFB) and a `AdminSportSwitcher` shared by both admin panels, plus a fix so CFB pools stop leaking into the golf admin list. Senior review (APPROVE WITH QUESTIONS, no blockers): a follow-up commit hardened the `?week` param and switched `getAllPools` to filter by `sport_id` (see DECISIONS, 2026-08-14). |
| 2026-08-14 | **Shipped: CFB PR10 — auto-fill on missed deadline + automatic lock/fill cron** (PR #56, `feat/cfb-autofill`). Closes the last core CFB build PR. `cfb.autofill_week()` drops a random valid card (5 ATS + 1 underdog, no double-down) into every empty week for a player who missed the deadline, and `cfb.process_locked_weeks()` — armed as an always-on, zero-cost `cfb-lock-autofill` pg_cron job, live in prod now — flips a week's status to `locked` and runs the fill automatically, with no admin action. This is separate from and ahead of the three billable CFBD pollers, which stay behind the PR9 admin toggle and are still not armed. Senior review caught a real bug pre-merge (a pick'em/spread-0 game has no underdog side, so the first draft could draw one into the mandatory underdog slot and drop that player from scoring — fixed) plus a fill-race condition (fixed with a per-week advisory lock); see DECISIONS, 2026-08-14. Only the PR9 prod cutover (billable pollers) is left on item **3.4**. |
| 2026-08-15 | **Shipped: CFB PR9 prod cutover + admin/picks UX pass** (PR #58, `cfb-admin-close-and-picks-ux`). Closes item **3.4**'s last open piece: all four CFB edge functions (`poll-cfb-lines`, `poll-cfb-scores`, `grade-cfb-week`, `cfd-proxy`) are deployed and all four CFB cron jobs — the three billable pollers plus the free `cfb-lock-autofill` — are armed and active in prod (verified via `cron.job`/`supabase functions list`, not just the migration diff). Cutover surfaced and fixed a real bug along the way: the polling toggle's `admin_stop_cfb_polling()`/`admin_cfb_polling_status()` matched any `cfb-%` job name, which collided with the always-on `cfb-lock-autofill` and had been silently disarming it every time an admin toggled billable polling off — both RPCs now scope to the three billable job names explicitly. Same PR added a "Close Pool" admin control (`setPoolStatus` moved to the sport-neutral `src/lib/pools.js` seam, shared by both admin panels), fixed a quarter-point spread-rounding bug (`chooseLine` now rounds to the nearest half-point; prod's ~38 affected rows were hand-corrected once, deliberately not captured as a migration — see DECISIONS), fixed closed CFB pools leaking into the Dashboard's active list, and reworked the weekly picks page (search/conference filter/sort, a redesigned inline underdog chip, a double-down effective-line preview, a "Card progress" tracker, a week dropdown, and a `?reset=1` edit flow that clears the card instead of pre-filling it — see DECISIONS) plus a new read-only weekly slate page. CFB is now fully live in prod; the item's only remaining gap is that no real user has run a season through it. |

---

*Sources for the market snapshot: [Splash Sports golf](https://splashsports.com/category/golf) · [Splash PGA pool FAQ](https://splashsports.com/faq/pga-pool) · [Splash One & Done](https://splashsports.com/blog/where-to-play-golf-one-and-done-contests) · [RunYourPool golf](https://www.runyourpool.com/golf/) · [RYP pricing](https://help.runyourpool.com/en/articles/5547004-what-is-the-cost-to-run-my-pool) · [Majors Challenge](https://www.majorschallenge.com/) · [Majors Challenge club product](https://landing.majorschallenge.com/golfclub.html) · [MC white-label launch](https://thegolfwire.com/majors-challenge-u-s-open-pools) · [RunMyPools golf](https://runmypools.com/golf/) · [EasyOfficePools](https://www.easyofficepools.com/personalized-pga-fantasy-golf-pools/)*
