import { StandingsCard } from 'poold';

export const Default = () => (
  <StandingsCard>
    <div style={{ padding: '14px 16px', fontSize: 13, color: '#736A5F' }}>
      Standings rows render inside this card shell (see the Standings component).
    </div>
  </StandingsCard>
);
